<partial name="_notificationView" />

<div class="container mx-auto px-4 py-6 rounded-lg bg-white shadow-md">
    <div class="flex flex-col items-center">
        <h1 class="text-2xl font-bold mb-4 text-gray-800">User Management</h1>

        <!-- Table Section with DataTable -->
        <div class="w-full overflow-x-auto rounded-lg bg-white p-3 shadow-lg border border-gray-200">
            <table id="mytable" class="w-full border-collapse">
                <thead>
                    <tr class="bg-gradient-to-r from-blue-500 to-blue-600 text-white">
                        <th class="px-6 py-3 text-left text-sm font-semibold uppercase">Name</th>
                        <th class="px-6 py-3 text-left text-sm font-semibold uppercase">Email</th>
                        <th class="px-6 py-3 text-left text-sm font-semibold uppercase">Phone Number</th>
                        <th class="px-6 py-3 text-left text-sm font-semibold uppercase">Company</th>
                        <th class="px-6 py-3 text-left text-sm font-semibold uppercase">Role</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-200">
                    <!-- Data will be dynamically loaded here by DataTable -->
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/user.js"></script>
}